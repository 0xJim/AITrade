# 回测策略版本全面梳理

## v5 — 无独立脚本
结果在 `data/backtest_v5_result.json`。无脚本可分析，跳过。

## v6 — 无独立脚本
结果在 `data/backtest_v6_result.json`。无脚本可分析，跳过。
（注：v6是历史最强策略之一，56笔/64.3%胜率/+$688，核心是极端费率信号）

---

## v7plus — 精准狙击版（开放参数）
**策略核心：** 基于v6数据优化的宽松版本。入场依赖极端费率信号（负费率做多/正费率做空）+ 环境评分（MIN_ENV_SCORE=3，较宽松）。ATR动态止损：低ATR币种SL=ATR×3.0，防止窄止损被扫。分批止盈50%后剩余仓位用EMA9趋势跟随，不设TP上限让利润奔跑。趋势跟随触发1.5%，移动止盈5%触发/4%回撤锁定。时间止损72h（延长自48h），36h开始衰减。
**出场：** SL止损（ATR动态）、分批止盈+趋势跟随、移动止盈、时间止损。
**仓位：** 固定RISK_PER_TRADE=1.2%，杠杆3倍，最多3仓位。
**与上一版差异：** v6→v7+：风险从1%提至1.2%，仓位从2增到3，冷却从24h降至12h，连亏冷却从7天降至4天，MIN_ENV_SCORE从4降至3，极端费率阈值放宽（-0.05/-0.08），关闭反转确认。

## v7tuned — 精准狙击版（收紧参数）
**策略核心：** 与v7plus相同的策略框架但参数更保守。ATR动态止损×3.0，低ATR宽止损防止被扫。启用连亏趋势反转确认（REVERSAL_CONFIRM_ENABLED=True），连亏后需5根反转K线才能重新入场。环境评分MIN_ENV_SCORE=5（更严格）。MAX_SL_PCT=8%（v7plus为15%），控制高ATR币种单笔亏损。趋势跟随触发3%（比v7plus的1.5%更晚）。分批止盈50%+保本。
**出场：** SL止损、分批止盈+趋势跟随、移动止盈、时间止损48h。
**仓位：** 固定RISK_PER_TRADE=1%，杠杆3倍，最多2仓位。
**与上一版差异：** v7+→v7tuned：风险降至1%，仓位降至2，ENV门槛升至5，MAX_SL收紧到8%，启用反转确认，趋势跟随触发从1.5%升至3%，时间止损从72h降至48h，连亏冷却7天。

## v8 — 六维加权评分版（参数外部化）
**策略核心：** 重大架构升级。引入六维加权评分系统（OI趋势20%/资金费率15%/量价25%/宏观环境15%/清算数据10%/聪明钱15%，总分-100~+100）替代原10维加法评分。信号质量评分(0-100)+三要素入场条件。Kelly公式动态仓位（保守系数0.25，默认胜率55%，仓位2-20%）。12种K线形态识别+多时间框架(1h/4h)一致性分析。参数从params.yaml外部加载，支持面板调参。
**出场：** ATR动态SL（×1.5）、分批止盈50%+保本、趋势跟随2%触发、移动止盈4%触发/2%回撤。
**仓位：** Kelly动态仓位，杠杆3倍，最多2仓位。
**与上一版差异：** v7→v8：全新六维评分替代加法评分，Kelly仓位替代固定风险，参数外部化(yaml)，K线形态识别，多时间框架一致性。减法优化：移除时间止损/日亏损上限/B级封杀/硬拒绝。

## v9 — 无独立脚本
结果在 `data/backtest_v9_result.json`。无脚本可分析，跳过。

## v10 — 三大升级版（15mK线+双向+费率异动）
**策略核心：** 三大升级：(1)真实15分钟K线数据，不再用1h近似；(2)完整双向交易，涨了做多+跌了做空；(3)费率异动作为独立触发条件。触发条件满足任一即可：A)单根15m K线涨跌幅>阈值(SPIKE_THRESHOLD=1%)；B)费率超出极端阈值（做多费率<-5%/做空费率>5%）。入场过滤：六维评分+多时间框架一致性+信号质量≥55+RR≥1.3+方向×质量×ATR过滤（做多ATR<5%+quality≥80，做空quality≥85+ATR≥3%）。Kelly仓位，10个时间框架分析。
**出场：** ATR动态SL（×1.5）、分批止盈50%+保本、趋势跟随、移动止盈。
**仓位：** Kelly动态（保守系数0.25），杠杆3倍，最多2仓位。
**与上一版差异：** v8→v10：新增15m真实K线、完整做空逻辑、费率异动独立触发、异动扫描器（1%阈值）、10个MTF框架。参数继续从yaml加载。

## v10c — 数据驱动优化版（当前主版本backtest.py）
**策略核心：** 基于v10b（39笔/+$958/19.2%）数据分析优化。核心发现：SL 3-4%区间90%胜率、做空71%胜率优于做多45%、持仓<12h全被SL扫亏$246、持仓>48h胜率88%。三大优化：(1)入场后4h宽限期（GRACE_PERIOD_HOURS=4），不检查SL；(2)MAX_HOLD延长到72h（原48h）；(3)初始资金$1000。ATR动态止损双档：低ATR×3.0/高ATR×1.5。MAX_SL_PCT=8%。做空env门槛+2。动态仓位：env≥7加成×1.2，仓位上限20%，每笔最大亏损2%。连亏反转确认+趋势跟随5%触发+分批止盈50%+保本+移动止盈5%/4%。
**出场：** ATR动态SL（含4h宽限期）、分批止盈+趋势跟随、移动止盈、时间止损72h。
**仓位：** 固定1.5%风险，env≥7加成×1.2，杠杆3倍，最多3仓位。
**与上一版差异：** v10→v10c：新增4h宽限期，MAX_HOLD 48→72h，风险2%→1.5%，仓位2→3，初始资金改为$1000，MAX_SL_PCT从5%→8%，env做空门槛调整。

## v11new (backtest_v11.py) — 深度数据分析优化版
**策略核心：** 基于v10c（2726笔/+$1184/2.7年）全面分析。核心发现：盈亏比0.59太低、75%交易未触发分批止盈净亏$17986、8h后止损587笔亏$28647。退出优化（P0）：分批触发从50%TP降至30%TP（多救~928笔）、8h时间止损（未触发分批的8h后收紧到保本+0.3%）、趋势跟随5%→3%更快启动、移动止盈5%→3%/回撤4%→2.5%更快锁利、取消4h宽限期。入场优化（P1）：MAX_SL_PCT从8%→5%、做多RSI>60、做空RSI<40、禁止下行趋势做多、做空仓位上限$200。
**出场：** ATR动态SL（无宽限期）、8h时间止损收紧至保本、分批止盈30%TP触发、趋势跟随3%、移动止盈3%/2.5%。
**仓位：** 固定1.5%风险，杠杆3倍，最多3仓位。
**与上一版差异：** v10c→v11：分批触发50→30%TP、8h时间止损（无分批触发则收紧至保本）、趋势跟随5→3%、移动止盈收紧、取消4h宽限期、MAX_SL 8→5%、新增RSI方向门槛（做多>60/做空<40）、禁止下行趋势做多、做空仓位上限$200。

## v11g — v11g仓位调整版（数据驱动，离线模拟）
**策略核心：** 完全不同于v11的优化路径。加载v10原始数据（535笔），应用v11基础过滤+v11g仓位调整进行离线模拟，不实时拉数据。v11基础：静态黑名单15币+V8≥4入场+做空V8≥5减半。v11g仓位调整（calc_position_multiplier）：(1)V8反转：V8≤4加仓×1.3，V8≥6.5减仓×0.6；(2)RSI区间：做多RSI<50×0.7，RSI65-75×1.2，RSI≥75×1.1；(3)SL区间：SL 4-6%×0.65，SL 8-10%×1.2；(4)连续亏损2笔×0.7。硬过滤：SL>10%跳过，ATR>5%跳过，V8≥6.5+RSI<55做多跳过。
**出场：** 使用v10原始出场结果，仅调整仓位。
**仓位：** 综合倍率 = 做空减半 × V8反转 × RSI区间 × SL区间 × 连亏冷却。
**与上一版差异：** v11→v11g：不同的优化路径——v11改退出逻辑，v11g改仓位管理。v11g是离线模拟（加载v10数据），不实时回测。核心创新是多因子仓位倍率系统。

## v11h — v11g归档后优化版
**策略核心：** 基于v11g回测数据（234笔/68.4%/+$2881/DD4.6%/PF2.08）拆解分析后的4个精准优化点。保持v11g全部参数基础，新增：(1)RSI 55-60做多减仓×0.7（v11g该区间50笔WR=58% PnL=-99U）；(2)ATR过滤边界从5.0%放宽到5.5%（恢复几笔ATR≈5%的赚钱交易）；(3)SKYAIUSDT减仓×0.8（50笔WR=56%量大但胜率低）；(4)做空V8≥6.5减仓×0.6（加固逻辑，仅1笔亏-19U）。
**出场/仓位：** 同v11g框架，叠加新减仓规则。
**与上一版差异：** v11g→v11h：新增RSI 55-60减仓、ATR过滤5.0→5.5%、SKYAIUSDT专项减仓、做空V8≥6.5额外减仓。精准数据驱动微调。

## v11i — v11g精简优化版（仅2处改动）
**策略核心：** 基于v11g数据的最小改动版。仅改2处：(1)V8≥6.5做多减仓×0.6→×0.8（v11g砍了+10847U利润，过于激进，回退至较温和的减仓）；(2)新增RSI 55-60做多减仓×0.4（v11g该区间50笔WR=58%亏-99U，用更激进的减仓）。其余完全保持v11g：黑名单15币、V8≥4入场、做空V8≥5减半、V8≤4加仓×1.3、RSI<50×0.7、RSI65-75×1.2、RSI≥75×1.1、SL 4-6%×0.65、SL 8-10%×1.2、连亏2笔×0.7。
**出场/仓位：** 同v11g框架。
**与上一版差异：** v11g→v11i：V8≥6.5做多减仓从×0.6放宽至×0.8（保留更多利润），新增RSI 55-60×0.4强减仓（比v11h的×0.7更激进）。追求最小改动最大收益。

## v12 (backtest_v12.py) — v12b调整版（RSI+仓位+信号排序）
**策略核心：** 基于v10c数据深度分析的3项优化：(1)做多RSI≥65跳过（仅过滤极端超买，修复v12初版误杀RSI55-64赚钱做多的问题）；(2)做多仓位系数0.5（做多胜率41% vs 做空62%，合理降低做多风险暴露）；(3)信号类型优先级排序：pump_short(71%) > extreme_pos_funding(62%) > coiling_breakout(54%) > crash_bounce(29%)。保留v10c全部参数框架（ATR动态止损、分批止盈、4h宽限期等）。
**出场：** 同v10c（ATR动态SL含4h宽限期、分批止盈50%+趋势跟随、移动止盈、时间止损72h）。
**仓位：** 固定1.5%风险 × 做多0.5倍系数，杠杆3倍，最多3仓位。
**与上一版差异：** v10c→v12：新增做多RSI≥65跳过、做多仓位0.5倍、信号优先级排序。本质是在v10c基础上的增量优化。

## v12j — V8分数硬过滤版（离线模拟）
**策略核心：** 基于v11g数据深度分析。核心发现：V8≥6.5的897笔共亏-2044U，V8<6.5的618笔净赚+5076U→V8高分是虚假信号，趋势可能已近末端。5项优化：(1)V8上限硬过滤：V8≥6.5直接跳过（最大改进+2044U）；(2)做多SL>5%减仓×0.6；(3)做空V8≥6.5也硬过滤（122笔亏-829U）；(4)连亏≥3×0.5（更激进冷却）；(5)保留v11g的RSI/SL调整。多版本AB测试（v12j-A到E）。
**出场：** 使用v10原始出场结果。
**仓位：** 基于v11g仓位框架，新增V8硬过滤和连亏×0.5。
**与上一版差异：** v11g→v12j：核心突破——发现V8≥6.5是虚假信号。新增V8上限硬过滤（最大单一改进），做多SL>5%减仓，做空V8≥6.5过滤，连亏3笔×0.5。

## v12j_v2 — V8分档精确优化版（离线模拟）
**策略核心：** 进一步细化V8分析。发现V8只有3个值：4.0(618笔+5076U)、6.5(884笔-2038U)、7.8(13笔-6U)。v11g的策略V8=6.5×0.6仍贡献-881U。多方案测试：G(6.5×0.3更激进减仓)、H(6.5直接过滤)、I(做多6.5×0.2/做空×0.0)、J(G+RSI<50×0.5+连亏≥3×0.4)、K(6.5仅保留做空×0.6做空WR=62.8%)。最终目标是找到最优V8处理方式。
**出场：** 使用v10原始出场结果。
**仓位：** V8分档倍率精确匹配。
**与上一版差异：** v12j→v12j_v2：从v11g多因子倍率简化为V8分档精确优化，多方案并行测试（G/K/J），追求V8=6.5的最优处理策略。

## v13 (backtest_v13.py) — v12b完整回测版（蓄势突破+暴涨回落）
**策略核心：** 与v12相同策略逻辑但作为完整独立回测脚本（v12只是参数文件描述）。信号类型包括：crash_bounce(崩盘反弹做多)、extreme_pos_funding(极端正费率做空)、coiling_breakout(蓄势突破多/空)、pump_short(暴涨回落做空)。入场：env≥4+六维评分+多框架一致性+做多RSI<65+ATR过滤。新增scan_coiling_breakout（4h周期扫描低波动蓄势后的突破）和scan_pump_short（检测短期暴涨后做空）。信号优先级：pump_short > extreme_pos_funding > coiling_breakout > crash_bounce。
**出场：** ATR动态SL（含4h宽限期）、分批止盈50%+趋势跟随、移动止盈5%/4%、时间止损72h。
**仓位：** 固定1.5%风险 × 做多0.5倍，杠杆3倍，最多3仓位。
**与上一版差异：** v12→v13：本质是v12b的完整回测实现，新增coiling_breakout和pump_short扫描器、信号优先级排序。增加了约350行代码（蓄势突破+暴涨回落扫描逻辑）。

## v14 — 3项数据驱动优化版
**策略核心：** 基于v12b数据（39笔/+$590/51.3%）分析亏损模式后3项优化：(1)ATR≥2%禁止做多（3笔全亏损$98，高ATR做多假突破多）；(2)低ATR(<0.7%)做空仓位上限$700（BNBUSDT+PAXGUSDT亏损$121，仓位$2100太大）；(3)env_score=5（BTC方向不匹配）仓位半价（1W/4L净亏$150）。保留v12b全部框架：RSI<65做多、做多仓位0.5倍、信号优先级排序、蓄势突破+暴涨回落扫描。
**出场：** 同v10c框架（ATR动态SL+4h宽限期+分批止盈+趋势跟随+移动止盈+时间止损72h）。
**仓位：** 1.5%风险 × 做多0.5 × env=5半价 × 低ATR做空$700上限。
**与上一版差异：** v13→v14：新增ATR≥2%禁止做多、低ATR做空仓位上限$700、env=5仓位半价。精准针对3类可识别亏损的定向修复。

## v15 — 数据驱动4项优化版
**策略核心：** 基于v14b数据（42笔/+$607/52.4%）分析后2项新优化（叠加v14原有）：(1)做空A级+ATR≥1.4%直接跳过（6笔净亏$202，仅WIFUSDT赚$90）；(2)ATR<0.7%做空直接跳过（2笔全亏$79，BNB+PAXG低波做空被反抽止损）。保留v14全部：ATR≥2%禁止做多、低ATR做空<$700、env=5半价、RSI<65、做多0.5倍、信号优先级、蓄势突破+暴涨回落。
**出场：** 同v10c框架。
**仓位：** 同v14 + 新增做空ATR过滤。
**与上一版差异：** v14→v15：新增做空A级+ATR≥1.4%跳过、ATR<0.7%做空跳过。进一步收紧做空入场条件。

## v16 — 回归v11策略核心：广撒网+动态淘汰
**策略核心：** 回归v11"广撒网"理念，放松v15的过度过滤。6处调整：(1)做多RSI放宽至75（v15为65，过度过滤）；(2)删除ATR≥2%禁做多（一刀切太严）；(3)做多仓位系数从0.5放宽至0.8；(4)删除env=5半价（过度压缩）；(5)删除ATR<0.7%做空跳过（改为$700仓位限制）；(6)删除做空A级+ATR≥1.4%跳过（6笔太少不具统计意义）。保留信号优先级、蓄势突破+暴涨回落、做空优先等核心逻辑。
**出场：** 同v10c框架。
**仓位：** 1.5%风险 × 做多0.8（放宽自0.5）。
**与上一版差异：** v15→v16：大幅放松过滤——RSI 65→75、做多0.5→0.8、删除ATR≥2%禁做多、删除env=5半价、删除2项做空跳过规则。理念从"严过滤"转向"广撒网+动态淘汰"。

## v17 — v6极端费率+v14蓄势突破 合并版
**策略核心：** 合并v6（56笔/64%/+$688）和v14（42笔/52%/+$607）两个已验证策略。v6核心是极端费率信号（45笔/64%胜率/+$462），v14核心是蓄势突破做空（62%胜率）。合并后：重新启用extreme_neg_funding做多+extreme_pos_funding做空+coiling_breakout+pump_short，保留v14所有数据驱动优化（RSI<65/ATR≥2%禁做多/做空仓位控制等）。MAX_POSITIONS从3增至4以容纳极端费率+蓄势突破并行。新增scan_extreme_neg_funding函数。极端费率做多不减半（自身64%胜率已验证）。
**出场：** 同v10c框架。
**仓位：** 1.5%风险 × 做多0.5（蓄势突破）/ 不减半（极端费率），最多4仓位。
**与上一版差异：** v16→v17：重新启用极端负费率信号（v6主力），MAX_POSITIONS 3→4，新增极端费率扫描器，极端费率做多不减半。策略合并而非单纯迭代。

## v18 — v6微调优化版
**策略核心：** 基于v6（56笔/64.3%/+$688）深度分析后的3项微调，不改v6核心逻辑。v6核心优势：极端费率信号、低SL(<5%)78%胜率、仓位$200-600区间胜率75-100%。v6核心问题：SL>8%交易44%胜率亏$104、DASH 9笔亏$223、SL<5%高胜率区间仓位太小。3项微调：(1)MAX_SL_PCT=8%（过滤宽止损交易，v6无上限导致SL=15%）；(2)单币累计亏$100后暂停该币2周（SYMBOL_LOSS_CAP，替代静态黑名单和次数限制）；(3)SL<5%仓位×1.5（低SL高胜率区间加大投入）。去掉静态黑名单，靠亏损上限动态管理。去掉亏过币种env门槛提高。
**出场：** ATR动态SL+趋势跟随+分批止盈+移动止盈+时间止损。SL>8%直接跳过。
**仓位：** 1.2%风险（v6原始） × SL<5%加仓×1.5，杠杆3倍，最多3仓位。
**与上一版差异：** v17→v18：回归纯v6核心（极端费率），去掉蓄势突破/暴涨回落等v14新增信号。3项精准微调：SL上限8%、单币亏损$100暂停、SL<5%加仓。去掉黑名单改为动态亏损管理。RISK恢复到v6的1.2%。
